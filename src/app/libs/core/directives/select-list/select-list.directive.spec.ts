// Directives
import { SelectListDirective } from './select-list.directive';

describe('SelectListDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectListDirective();
    expect(directive).toBeTruthy();
  });
});
