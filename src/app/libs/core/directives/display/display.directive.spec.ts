// Directives
import { DisplayDirective } from './display.directive';

describe('DisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
