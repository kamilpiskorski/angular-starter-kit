// Directives
import { JumpDirective } from './jump.directive';

describe('JumpDirective', () => {
  it('should create an instance', () => {
    const directive = new JumpDirective();
    expect(directive).toBeTruthy();
  });
});
