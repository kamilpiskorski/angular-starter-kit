// Directives
import { IfMatchMediaDirective } from './if-match-media.directive';

describe('IfMatchMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new IfMatchMediaDirective(null, null, null, null);
    expect(directive).toBeTruthy();
  });
});
