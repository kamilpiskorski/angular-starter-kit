// Directives
import { SelectOptionDirective } from './select-option.directive';

describe('SelectOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOptionDirective(null);
    expect(directive).toBeTruthy();
  });
});
