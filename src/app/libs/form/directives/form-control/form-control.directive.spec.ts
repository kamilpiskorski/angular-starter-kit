// Directives
import { FormControlDirective } from './form-control.directive';

describe('FormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlDirective(null);
    expect(directive).toBeTruthy();
  });
});
