// Directives
import { FormLabelDirective } from './form-label.directive';

describe('FormLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new FormLabelDirective(null);
    expect(directive).toBeTruthy();
  });
});
